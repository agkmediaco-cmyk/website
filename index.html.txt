<!DOCTYPE html>
<html lang="sv">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Daisy's Råsunda — Burgers & Vibes</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Permanent+Marker&family=Nunito:wght@400;600;800&display=swap" rel="stylesheet">
<style>
  :root {
    --red: #D91A2A;
    --red-bright: #FF2D3F;
    --red-dark: #8B0000;
    --black: #0D0D0D;
    --black2: #1A1A1A;
    --black3: #242424;
    --white: #F5F0EE;
    --gray: #3A3A3A;
    --grad-main: linear-gradient(135deg, #D91A2A 0%, #6B0000 100%);
    --grad-fire: linear-gradient(180deg, #FF2D3F 0%, #8B0000 60%, #0D0D0D 100%);
    --grad-dark: linear-gradient(135deg, #1A1A1A 0%, #0D0D0D 100%);
    --grad-accent: linear-gradient(90deg, #D91A2A, #FF2D3F, #D91A2A);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    background: var(--black);
    color: var(--white);
    font-family: 'Nunito', sans-serif;
    overflow-x: hidden;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    background: var(--black);
    border-bottom: 3px solid var(--red);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 2rem;
    height: 64px;
  }

  .nav-logo {
    font-family: 'Permanent Marker', cursive;
    background: var(--grad-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 1.6rem;
    letter-spacing: 1px;
    text-decoration: none;
  }

  .nav-links { display: flex; gap: 2rem; list-style: none; }

  .nav-links a {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.1rem;
    letter-spacing: 2px;
    color: var(--white);
    text-decoration: none;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--red-bright); }

  /* HERO */
  .hero {
    min-height: 100vh;
    background: var(--black);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    padding-top: 64px;
  }

  /* Gradient glow backdrop */
  .hero-glow {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 70% 60% at 50% 80%, rgba(180,0,0,0.35) 0%, transparent 70%),
      radial-gradient(ellipse 40% 30% at 20% 20%, rgba(217,26,42,0.12) 0%, transparent 60%);
    pointer-events: none;
  }

  /* Checkerboard strip */
  .hero::before {
    content: '';
    position: absolute;
    top: 64px; left: 0; right: 0;
    height: 24px;
    background-image: repeating-linear-gradient(
      90deg,
      var(--red) 0px, var(--red) 24px,
      var(--black) 24px, var(--black) 48px
    );
    z-index: 1;
  }

  .hero::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 24px;
    background-image: repeating-linear-gradient(
      90deg,
      var(--black) 0px, var(--black) 24px,
      var(--red-dark) 24px, var(--red-dark) 48px
    );
  }

  /* Retro grid */
  .retro-grid {
    position: absolute;
    bottom: 24px; left: 0; right: 0;
    height: 45%;
    background-image:
      linear-gradient(rgba(217,26,42,0.18) 1px, transparent 1px),
      linear-gradient(90deg, rgba(217,26,42,0.18) 1px, transparent 1px);
    background-size: 60px 60px;
    transform: perspective(400px) rotateX(45deg);
    transform-origin: bottom;
    mask-image: linear-gradient(to top, black 0%, transparent 100%);
  }

  /* Starburst */
  .starburst {
    position: absolute;
    top: 12%; right: 8%;
    width: 140px; height: 140px;
    background: var(--grad-main);
    clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
    display: flex;
    align-items: center;
    justify-content: center;
    animation: spin-slow 20s linear infinite;
    z-index: 2;
  }

  .starburst span {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.9rem;
    letter-spacing: 1px;
    text-align: center;
    color: var(--white);
    line-height: 1.2;
  }

  @keyframes spin-slow {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    padding: 60px 2rem 120px;
  }

  .hero-eyebrow {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1rem;
    letter-spacing: 6px;
    color: var(--red-bright);
    margin-bottom: 1rem;
    animation: fadeUp 0.6s ease both;
  }

  .hero-title {
    font-family: 'Permanent Marker', cursive;
    font-size: clamp(4rem, 12vw, 9rem);
    background: var(--grad-fire);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 0.9;
    filter: drop-shadow(4px 4px 0 rgba(139,0,0,0.8));
    margin-bottom: 1.5rem;
    animation: fadeUp 0.6s ease 0.15s both;
  }

  .hero-title .line2 {
    background: linear-gradient(90deg, #fff 0%, #ccc 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: clamp(2rem, 6vw, 4.5rem);
    display: block;
    filter: drop-shadow(3px 3px 0 rgba(217,26,42,0.6));
  }

  .hero-sub {
    font-size: 1.1rem;
    font-weight: 600;
    color: rgba(245,240,238,0.65);
    margin-bottom: 2.5rem;
    letter-spacing: 1px;
    animation: fadeUp 0.6s ease 0.3s both;
  }

  .hero-ctas {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeUp 0.6s ease 0.45s both;
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .btn {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.1rem;
    letter-spacing: 3px;
    padding: 14px 36px;
    border: none;
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    transition: transform 0.15s, box-shadow 0.15s, filter 0.15s;
  }

  .btn:hover { transform: translate(-2px, -2px); }
  .btn:active { transform: translate(0,0); }

  .btn-primary {
    background: var(--grad-main);
    color: var(--white);
    box-shadow: 4px 4px 0 var(--red-dark);
  }
  .btn-primary:hover { box-shadow: 6px 6px 0 var(--red-dark); filter: brightness(1.1); }

  .btn-secondary {
    background: transparent;
    color: var(--white);
    border: 2px solid var(--red);
    box-shadow: 4px 4px 0 var(--red-dark);
  }
  .btn-secondary:hover { box-shadow: 6px 6px 0 var(--red-dark); border-color: var(--red-bright); }

  /* TICKER TAPE */
  .tape {
    height: 48px;
    background: var(--grad-main);
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .tape-text {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1rem;
    letter-spacing: 8px;
    color: var(--white);
    white-space: nowrap;
    animation: ticker 20s linear infinite;
  }

  @keyframes ticker {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  /* ABOUT */
  .about {
    background: var(--black2);
    padding: 6rem 2rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    max-width: 1100px;
    margin: 0 auto;
    align-items: center;
  }

  .about-badge {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.85rem;
    letter-spacing: 4px;
    color: var(--red-bright);
    margin-bottom: 0.75rem;
  }

  .section-title {
    font-family: 'Permanent Marker', cursive;
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    color: var(--white);
    line-height: 1.05;
    margin-bottom: 1.25rem;
  }

  .section-title .accent {
    background: var(--grad-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .about-text {
    font-size: 1.05rem;
    line-height: 1.8;
    color: rgba(245,240,238,0.65);
    margin-bottom: 1.5rem;
  }

  .rating-pill {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--grad-main);
    color: var(--white);
    padding: 10px 20px;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.1rem;
    letter-spacing: 2px;
  }

  .about-card {
    background: var(--grad-dark);
    border: 1px solid rgba(217,26,42,0.3);
    padding: 3rem 2.5rem;
    position: relative;
    text-align: center;
  }

  .about-card::before {
    content: '';
    position: absolute;
    inset: -8px;
    border: 2px dashed rgba(217,26,42,0.4);
    pointer-events: none;
  }

  .about-card-emoji {
    font-size: 5rem;
    display: block;
    margin-bottom: 1rem;
    animation: wiggle 3s ease-in-out infinite;
  }

  @keyframes wiggle {
    0%, 100% { transform: rotate(-5deg); }
    50% { transform: rotate(5deg); }
  }

  .about-card-title {
    font-family: 'Permanent Marker', cursive;
    background: var(--grad-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 1.8rem;
    margin-bottom: 0.5rem;
  }

  .about-card-sub { color: rgba(245,240,238,0.55); font-size: 0.95rem; }

  .floating-badge {
    position: absolute;
    bottom: -20px; right: -20px;
    background: var(--grad-main);
    color: var(--white);
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.9rem;
    letter-spacing: 2px;
    padding: 12px 18px;
    box-shadow: 4px 4px 0 var(--black);
  }

  /* MENU */
  .menu-section {
    background: var(--black);
    padding: 6rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .menu-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 50% 40% at 0% 100%, rgba(139,0,0,0.15) 0%, transparent 60%),
      radial-gradient(ellipse 40% 40% at 100% 0%, rgba(217,26,42,0.08) 0%, transparent 60%);
    pointer-events: none;
  }

  .menu-header { text-align: center; margin-bottom: 3.5rem; position: relative; }

  .menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .menu-card {
    background: linear-gradient(160deg, #1e1e1e 0%, #111 100%);
    border: 1px solid rgba(217,26,42,0.25);
    padding: 2rem 1.5rem;
    position: relative;
    transition: transform 0.2s, border-color 0.2s, box-shadow 0.2s;
    cursor: default;
  }

  .menu-card:hover {
    transform: translateY(-6px);
    border-color: var(--red);
    box-shadow: 0 8px 40px rgba(217,26,42,0.2);
  }

  .menu-card-icon { font-size: 3rem; display: block; margin-bottom: 1rem; }

  .menu-card-tag {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.75rem;
    letter-spacing: 3px;
    color: var(--red-bright);
    margin-bottom: 0.5rem;
  }

  .menu-card-name {
    font-family: 'Permanent Marker', cursive;
    font-size: 1.5rem;
    color: var(--white);
    margin-bottom: 0.75rem;
  }

  .menu-card-desc {
    font-size: 0.9rem;
    color: rgba(245,240,238,0.5);
    line-height: 1.6;
    margin-bottom: 1.25rem;
  }

  .menu-card-price {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.4rem;
    background: var(--grad-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: 2px;
  }

  .menu-card-corner {
    position: absolute;
    top: 0; right: 0;
    width: 0; height: 0;
    border-style: solid;
    border-width: 0 36px 36px 0;
    border-color: transparent var(--red) transparent transparent;
  }

  /* INFO STRIP */
  .info-strip {
    background: var(--grad-main);
    padding: 5rem 2rem;
    position: relative;
    overflow: hidden;
  }

  .info-strip::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0,0,0,0.3) 0%, transparent 50%, rgba(0,0,0,0.3) 100%);
  }

  .info-strip::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 6px;
    background-image: repeating-linear-gradient(
      90deg,
      rgba(0,0,0,0.4) 0px, rgba(0,0,0,0.4) 18px,
      transparent 18px, transparent 36px
    );
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 3rem;
    max-width: 1000px;
    margin: 0 auto;
    position: relative;
  }

  .info-block { text-align: center; }
  .info-icon { font-size: 2.5rem; display: block; margin-bottom: 1rem; }

  .info-label {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.8rem;
    letter-spacing: 4px;
    color: rgba(245,240,238,0.65);
    margin-bottom: 0.5rem;
  }

  .info-value {
    font-family: 'Permanent Marker', cursive;
    font-size: 1.3rem;
    color: var(--white);
    line-height: 1.5;
  }

  .info-sub { font-size: 0.9rem; color: rgba(245,240,238,0.65); margin-top: 0.25rem; }

  /* HOURS */
  .hours-section {
    background: var(--black2);
    padding: 6rem 2rem;
  }

  .hours-inner { max-width: 700px; margin: 0 auto; text-align: center; }

  .hours-grid {
    margin-top: 2.5rem;
    background: var(--black);
    border: 1px solid rgba(217,26,42,0.2);
    overflow: hidden;
  }

  .hours-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 2rem;
    border-bottom: 1px solid rgba(217,26,42,0.1);
    transition: background 0.2s;
  }

  .hours-row:last-child { border-bottom: none; }
  .hours-row:hover { background: #1a1a1a; }

  .hours-day {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1rem;
    letter-spacing: 2px;
    color: var(--white);
  }

  .hours-time { font-size: 0.95rem; font-weight: 600; color: var(--red-bright); }

  .hours-open {
    width: 8px; height: 8px;
    background: var(--red-bright);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
    box-shadow: 0 0 6px var(--red);
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(1.5); }
  }

  .today-row {
    background: rgba(217,26,42,0.08);
    border-left: 4px solid var(--red);
  }

  /* ORDER */
  .order-section {
    background: var(--black);
    padding: 5rem 2rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .order-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 80% 60% at 50% 50%, rgba(139,0,0,0.25) 0%, transparent 70%);
  }

  .order-section::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 4px;
    background: var(--grad-main);
  }

  .order-title {
    font-family: 'Permanent Marker', cursive;
    font-size: clamp(2rem, 6vw, 4rem);
    background: var(--grad-fire);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1rem;
    position: relative;
    filter: drop-shadow(0 0 20px rgba(217,26,42,0.4));
  }

  .order-sub {
    font-size: 1.1rem;
    color: rgba(245,240,238,0.6);
    margin-bottom: 2.5rem;
    position: relative;
  }

  .order-btns { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; position: relative; }

  .btn-white {
    background: var(--white);
    color: var(--black);
    box-shadow: 4px 4px 0 var(--red-dark);
  }
  .btn-white:hover { box-shadow: 6px 6px 0 var(--red-dark); }

  .btn-outline-white {
    background: transparent;
    color: var(--white);
    border: 2px solid rgba(245,240,238,0.5);
    box-shadow: 4px 4px 0 rgba(217,26,42,0.5);
  }
  .btn-outline-white:hover { border-color: var(--white); box-shadow: 6px 6px 0 rgba(217,26,42,0.7); }

  /* CONTACT */
  .contact-section { background: var(--black2); padding: 6rem 2rem; }

  .contact-inner {
    max-width: 900px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .contact-list { list-style: none; margin-top: 2rem; display: flex; flex-direction: column; gap: 1.25rem; }

  .contact-item { display: flex; align-items: flex-start; gap: 1rem; }

  .contact-item-icon {
    width: 40px; height: 40px;
    background: var(--grad-main);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    flex-shrink: 0;
  }

  .contact-item-label {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.8rem;
    letter-spacing: 3px;
    color: var(--red-bright);
    margin-bottom: 2px;
  }

  .contact-item-value {
    color: var(--white);
    font-size: 0.95rem;
    line-height: 1.5;
    text-decoration: none;
    transition: color 0.2s;
  }

  .contact-item-value:hover { color: var(--red-bright); }

  .map-box {
    background: linear-gradient(160deg, #1e1e1e, #0d0d0d);
    border: 1px solid rgba(217,26,42,0.3);
    padding: 2rem;
    text-align: center;
  }

  .map-placeholder {
    height: 220px;
    background: linear-gradient(135deg, #1a0000 0%, #0d0d0d 100%);
    border: 1px solid rgba(217,26,42,0.15);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }

  .map-pin { font-size: 3rem; animation: bounce 2s ease-in-out infinite; }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  .map-label {
    font-family: 'Bebas Neue', sans-serif;
    color: rgba(245,240,238,0.35);
    font-size: 0.8rem;
    letter-spacing: 3px;
    margin-top: 0.5rem;
  }

  /* FOOTER */
  footer {
    background: var(--black);
    padding: 2.5rem 2rem;
    text-align: center;
    border-top: 3px solid var(--red-dark);
    position: relative;
  }

  footer::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
    background: var(--grad-main);
  }

  .footer-logo {
    font-family: 'Permanent Marker', cursive;
    background: var(--grad-main);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 2rem;
    display: block;
    margin-bottom: 0.75rem;
  }

  .footer-tagline {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 0.85rem;
    letter-spacing: 4px;
    color: rgba(245,240,238,0.3);
    margin-bottom: 1.5rem;
  }

  .footer-copy { font-size: 0.8rem; color: rgba(245,240,238,0.2); }

  /* SECTION WRAPPER */
  .about-wrapper { background: var(--black2); }

  /* RESPONSIVE */
  @media (max-width: 700px) {
    .about { grid-template-columns: 1fr; gap: 2rem; }
    .contact-inner { grid-template-columns: 1fr; gap: 2.5rem; }
    .starburst { width: 90px; height: 90px; top: 8%; right: 4%; }
    .starburst span { font-size: 0.65rem; }
    .nav-links { display: none; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">Daisy's 🌼</a>
  <ul class="nav-links">
    <li><a href="#menu">Meny</a></li>
    <li><a href="#hours">Öppettider</a></li>
    <li><a href="#contact">Hitta oss</a></li>
    <li><a href="#order" class="btn btn-primary" style="padding:8px 20px; font-size:0.9rem;">Beställ nu</a></li>
  </ul>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-glow"></div>
  <div class="retro-grid"></div>

  <div class="starburst">
    <span>4.9 ★<br>RATING</span>
  </div>

  <div class="hero-content">
    <p class="hero-eyebrow">★ Råsundavägen 92, Solna ★</p>
    <h1 class="hero-title">
      Daisy's
      <span class="line2">Råsunda</span>
    </h1>
    <p class="hero-sub">Retro vibes. Real food. 80s &amp; 90s all day long.</p>
    <div class="hero-ctas">
      <a href="#order" class="btn btn-primary">🍔 Beställ Pickup</a>
      <a href="#menu" class="btn btn-secondary">Se Menyn</a>
    </div>
  </div>
</section>

<!-- TICKER -->
<div class="tape">
  <div class="tape-text">
    🍔 BURGERS &nbsp;&nbsp;★&nbsp;&nbsp; 🍟 FRIES &nbsp;&nbsp;★&nbsp;&nbsp; 🥤 SHAKES &nbsp;&nbsp;★&nbsp;&nbsp; 🌭 HOTDOGS &nbsp;&nbsp;★&nbsp;&nbsp; 🧅 ONION RINGS &nbsp;&nbsp;★&nbsp;&nbsp; ⚡ ÖPPET VARJE DAG &nbsp;&nbsp;★&nbsp;&nbsp; 🍔 BURGERS &nbsp;&nbsp;★&nbsp;&nbsp; 🍟 FRIES &nbsp;&nbsp;★&nbsp;&nbsp; 🥤 SHAKES &nbsp;&nbsp;★&nbsp;&nbsp; 🌭 HOTDOGS &nbsp;&nbsp;★&nbsp;&nbsp; 🧅 ONION RINGS &nbsp;&nbsp;★&nbsp;&nbsp; ⚡ ÖPPET VARJE DAG &nbsp;&nbsp;★&nbsp;&nbsp;
  </div>
</div>

<!-- ABOUT -->
<section class="about-wrapper">
  <div class="about">
    <div>
      <p class="about-badge">★ OM OSS</p>
      <h2 class="section-title">Välkommen till <span class="accent">Daisy's</span></h2>
      <p class="about-text">Vi är Råsundas mest nostalgiska matställe — ett ställe där 80- och 90-talets bästa vibes möter riktigt god mat. Allt vi lagar är gjort med kärlek, bra råvaror och ett leende.</p>
      <p class="about-text">Oavsett om du svänger förbi för en snabb lunch eller stannar för en hela kvällen, lovar vi att du lämnar med full mage och ett stort leende.</p>
      <div style="display:flex; gap:1rem; flex-wrap:wrap; align-items:center;">
        <span class="rating-pill">★★★★★ &nbsp;4.9 på Google</span>
        <span style="font-size:0.9rem; color:rgba(245,240,238,0.4);">7 recensioner</span>
      </div>
    </div>
    <div style="position:relative;">
      <div class="about-card">
        <span class="about-card-emoji">🌼</span>
        <div class="about-card-title">Est. i Råsunda</div>
        <div class="about-card-sub">Solnas retro-favorit sedan dag ett</div>
      </div>
      <div class="floating-badge">ÄKTA KVALITET</div>
    </div>
  </div>
</section>

<!-- MENU -->
<section class="menu-section" id="menu">
  <div class="menu-header">
    <p class="about-badge">★ VÅR MENY</p>
    <h2 class="section-title">Välj din <span class="accent">favorit</span></h2>
  </div>
  <div class="menu-grid">
    <div class="menu-card">
      <div class="menu-card-corner"></div>
      <span class="menu-card-icon">🍔</span>
      <div class="menu-card-tag">SIGNATUR</div>
      <div class="menu-card-name">Daisy Burger</div>
      <div class="menu-card-desc">Vår klassiker med dubbel patty, hemgjord sås, karamelliserad lök och brioche-bröd.</div>
      <div class="menu-card-price">135 kr</div>
    </div>
    <div class="menu-card">
      <div class="menu-card-corner"></div>
      <span class="menu-card-icon">🌶️</span>
      <div class="menu-card-tag">HETT VAL</div>
      <div class="menu-card-name">Spicy 90s</div>
      <div class="menu-card-desc">Kryddig jalapeño-burger med chipotle-mayo och het salsasås. Töser tårar sedan 1994.</div>
      <div class="menu-card-price">145 kr</div>
    </div>
    <div class="menu-card">
      <div class="menu-card-corner"></div>
      <span class="menu-card-icon">🐔</span>
      <div class="menu-card-tag">CHICKEN</div>
      <div class="menu-card-name">Crispy Chick</div>
      <div class="menu-card-desc">Krispig panerad kyckling med coleslaw, pickles och honungssenap på potatisbröd.</div>
      <div class="menu-card-price">129 kr</div>
    </div>
    <div class="menu-card">
      <div class="menu-card-corner"></div>
      <span class="menu-card-icon">🍟</span>
      <div class="menu-card-tag">SIDOR</div>
      <div class="menu-card-name">Loaded Fries</div>
      <div class="menu-card-desc">Tjocka pommes toppad med ost, bacon, jalapeños och vår hemliga dippsås.</div>
      <div class="menu-card-price">89 kr</div>
    </div>
    <div class="menu-card">
      <div class="menu-card-corner"></div>
      <span class="menu-card-icon">🥤</span>
      <div class="menu-card-tag">DRYCK</div>
      <div class="menu-card-name">Retro Shake</div>
      <div class="menu-card-desc">Tjocka milkshakes i jordgubb, choklad eller vanilj. Raka 80-talskänslan.</div>
      <div class="menu-card-price">79 kr</div>
    </div>
    <div class="menu-card">
      <div class="menu-card-corner"></div>
      <span class="menu-card-icon">🧅</span>
      <div class="menu-card-tag">KLASSIKER</div>
      <div class="menu-card-name">Onion Rings</div>
      <div class="menu-card-desc">Gyllenbruna lökringar med krispig panering. Perfekt kombination med burgaren.</div>
      <div class="menu-card-price">65 kr</div>
    </div>
  </div>
</section>

<!-- INFO STRIP -->
<div class="info-strip">
  <div class="info-grid">
    <div class="info-block">
      <span class="info-icon">📍</span>
      <div class="info-label">ADRESS</div>
      <div class="info-value">Råsundavägen 92<br>169 57 Solna</div>
    </div>
    <div class="info-block">
      <span class="info-icon">📞</span>
      <div class="info-label">TELEFON</div>
      <div class="info-value"><a href="tel:0722720720" style="color:var(--white);text-decoration:none;">072-272 07 20</a></div>
      <div class="info-sub">Ring oss för catering & events</div>
    </div>
    <div class="info-block">
      <span class="info-icon">⏰</span>
      <div class="info-label">ÖPPETTIDER</div>
      <div class="info-value">10:00 – 22:00</div>
      <div class="info-sub">Alla dagar i veckan</div>
    </div>
  </div>
</div>

<!-- HOURS -->
<section class="hours-section" id="hours">
  <div class="hours-inner">
    <p class="about-badge">★ ÖPPETTIDER</p>
    <h2 class="section-title">Vi är <span class="accent">öppna</span> varje dag</h2>
    <div class="hours-grid">
      <div class="hours-row" id="row-mon">
        <span class="hours-day">Måndag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-mon" style="opacity:0;"></div>
        </div>
      </div>
      <div class="hours-row" id="row-tue">
        <span class="hours-day">Tisdag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-tue" style="opacity:0;"></div>
        </div>
      </div>
      <div class="hours-row" id="row-wed">
        <span class="hours-day">Onsdag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-wed" style="opacity:0;"></div>
        </div>
      </div>
      <div class="hours-row" id="row-thu">
        <span class="hours-day">Torsdag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-thu" style="opacity:0;"></div>
        </div>
      </div>
      <div class="hours-row" id="row-fri">
        <span class="hours-day">Fredag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-fri" style="opacity:0;"></div>
        </div>
      </div>
      <div class="hours-row" id="row-sat">
        <span class="hours-day">Lördag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-sat" style="opacity:0;"></div>
        </div>
      </div>
      <div class="hours-row" id="row-sun">
        <span class="hours-day">Söndag</span>
        <div style="display:flex;align-items:center;gap:12px;">
          <span class="hours-time">10:00 – 22:00</span>
          <div class="hours-open" id="dot-sun" style="opacity:0;"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ORDER -->
<section class="order-section" id="order">
  <h2 class="order-title">Hungrig? Beställ nu!</h2>
  <p class="order-sub">Hämta själv eller få det hemlevererat. Enkelt, snabbt, gott.</p>
  <div class="order-btns">
    <a href="tel:0722720720" class="btn btn-primary">📞 Ring & Beställ</a>
    <a href="https://share.google/85sNW2xGDxQdNTz8R" target="_blank" class="btn btn-outline-white">🗺 Se på Google Maps</a>
  </div>
</section>

<!-- CONTACT -->
<section class="contact-section" id="contact">
  <div class="contact-inner">
    <div>
      <p class="about-badge">★ HITTA OSS</p>
      <h2 class="section-title">Kom och <span class="accent">hälsa på</span></h2>
      <ul class="contact-list">
        <li class="contact-item">
          <div class="contact-item-icon">📍</div>
          <div>
            <div class="contact-item-label">ADRESS</div>
            <a href="https://www.google.com/maps/search/Råsundavägen+92+Solna" target="_blank" class="contact-item-value">Råsundavägen 92<br>169 57 Solna</a>
          </div>
        </li>
        <li class="contact-item">
          <div class="contact-item-icon">📞</div>
          <div>
            <div class="contact-item-label">TELEFON</div>
            <a href="tel:0722720720" class="contact-item-value">072-272 07 20</a>
          </div>
        </li>
        <li class="contact-item">
          <div class="contact-item-icon">⏰</div>
          <div>
            <div class="contact-item-label">ÖPPETTIDER</div>
            <span class="contact-item-value">Mån – Sön: 10:00 – 22:00</span>
          </div>
        </li>
      </ul>
    </div>
    <div class="map-box">
      <div class="map-placeholder">
        <span class="map-pin">📍</span>
        <div class="map-label">RÅSUNDAVÄGEN 92, SOLNA</div>
      </div>
      <a href="https://share.google/85sNW2xGDxQdNTz8R" target="_blank" class="btn btn-primary" style="width:100%;text-align:center;display:block;">
        🗺 Öppna i Google Maps
      </a>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <span class="footer-logo">Daisy's 🌼</span>
  <div class="footer-tagline">★ Råsunda's Retro Fast Food ★</div>
  <div class="footer-copy">© 2025 Daisy's Råsunda · Råsundavägen 92, 169 57 Solna · 072-272 07 20</div>
</footer>

<script>
  const days = ['sun','mon','tue','wed','thu','fri','sat'];
  const today = days[new Date().getDay()];
  const todayRow = document.getElementById('row-' + today);
  const todayDot = document.getElementById('dot-' + today);
  if (todayRow) todayRow.classList.add('today-row');
  if (todayDot) todayDot.style.opacity = '1';

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.style.opacity = '1';
        e.target.style.transform = 'translateY(0)';
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.menu-card, .info-block, .about-card').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(20px)';
    el.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
    observer.observe(el);
  });
</script>
</body>
</html>